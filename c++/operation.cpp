#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    // cout<<sqrt(36);     it is used for find the square root of any number
    // cout<<pow(3,3);     it is used for find the power of any number
    // cout<<round(4.3);   
    // cout<<round(5.8);   its round the nearest number
    // cout<<ceil(4.1);    its round the number into the next hieghest whole number
    // cout<<floor(6.9);      its is basically going to round down 
    // cout<<fmax(3,10);      its can found which can be bigger
    cout<<fmin(3,10);        




    return 0;
}

